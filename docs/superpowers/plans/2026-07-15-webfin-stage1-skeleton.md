# WebFin — Етап 1 «Каркас»: план реалізації

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Працюючий каркас WebFin: Next.js-додаток з реєстрацією/входом через Supabase, захищеними сторінками та анімованим сайдбаром.

**Architecture:** Next.js App Router (фронтенд + серверні роути в одному проєкті). Supabase — база даних (Postgres, RLS на всіх таблицях) і автентифікація (email + пароль, сесія в cookies через @supabase/ssr). Middleware перевіряє сесію і перенаправляє неавторизованих на /login.

**Tech Stack:** Next.js 15 (TypeScript, App Router, src-dir), Tailwind CSS, @supabase/supabase-js + @supabase/ssr, lucide-react (іконки), Vitest (юніт-тести), npm.

**Специфікація:** `docs/superpowers/specs/2026-07-15-webfin-design.md`

## Global Constraints

- Мова всього інтерфейсу — українська; тема — світла (фон `bg-slate-50`, картки білі, акцент emerald).
- Секрети лише в `.env.local` (входить у `.gitignore`); секретні значення ніколи не комітяться і не виводяться в консоль.
- Row Level Security увімкнено на **всіх** таблицях.
- Грошові суми в базі — `bigint` у копійках; відʼємне значення = витрата.
- Пакетний менеджер — npm. Директорія проєкту — `/Users/macbook/WEB` (git-репозиторій уже створено, у ньому є `docs/`).
- Кроки, позначені **🧑 ДІЯ КОРИСТУВАЧА (Yana)**, виконує користувач — новачок, не розробник. Виконавець зупиняється, передає їй інструкцію в чат дослівно (українською), чекає на підтвердження і лише потім продовжує.

---

### Task 1: Каркас Next.js-проєкту

**Files:**
- Create: увесь каркас Next.js у корені `/Users/macbook/WEB` (через create-next-app)
- Modify: `src/app/layout.tsx`

**Interfaces:**
- Consumes: —
- Produces: робочий Next.js-проєкт зі скриптами `npm run dev`, `npm run build`; alias `@/*` → `src/*`; Tailwind підключений у `src/app/globals.css`

- [ ] **Step 1: Згенерувати проєкт у тимчасову папку**

Корінь не порожній (`docs/`, `.git`), тому create-next-app туди напряму не встане — генеруємо поруч і переносимо:

```bash
cd /Users/macbook/WEB
npx create-next-app@15 webfin-tmp --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm
```

Expected: завершується без помилок, зʼявляється папка `webfin-tmp` з `package.json`, `src/app/`. (Пінимо мажорну версію 15, щоб конвенція `middleware.ts` була стабільною.)

- [ ] **Step 2: Перенести файли в корінь і прибрати тимчасову папку**

```bash
cd /Users/macbook/WEB
rsync -a webfin-tmp/ ./
rm -rf webfin-tmp
```

Expected: у корені зʼявились `package.json`, `src/`, `node_modules/`, `.gitignore`; папка `webfin-tmp` зникла.

- [ ] **Step 3: Перевірити, що дев-сервер запускається**

```bash
npm run dev
```

Expected: `Ready in ...`, сторінка http://localhost:3000 відкривається зі стартовим екраном Next.js. Після перевірки зупинити сервер (Ctrl+C).

- [ ] **Step 4: Українська мова і назва в кореневому layout**

У `src/app/layout.tsx` замінити:

```tsx
<html lang="en">
```

на:

```tsx
<html lang="uk">
```

і замінити:

```tsx
export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};
```

на:

```tsx
export const metadata: Metadata = {
  title: "WebFin — аналітика витрат",
  description: "Фінансова аналітика ваших витрат з Monobank",
};
```

- [ ] **Step 5: Перевірити збірку і закомітити**

```bash
npm run build
```

Expected: `Compiled successfully`.

```bash
git add -A
git commit -m "feat: каркас Next.js-проєкту WebFin (TypeScript, Tailwind, App Router)"
```

---

### Task 2: Vitest і модуль маршрутів (TDD)

**Files:**
- Create: `vitest.config.ts`, `src/lib/routes.ts`
- Test: `src/lib/routes.test.ts`
- Modify: `package.json` (script `test`)

**Interfaces:**
- Consumes: alias `@/*` з Task 1
- Produces:
  - `NAV_ITEMS: { href: string; label: string }[]` — пункти меню (використовує Task 7)
  - `isProtectedPath(pathname: string): boolean` — чи потрібна авторизація (використовує Task 5)
  - `isAuthPath(pathname: string): boolean` — чи це /login або /register (використовує Task 5)

- [ ] **Step 1: Встановити Vitest і додати скрипт**

```bash
npm i -D vitest
```

У `package.json` в `"scripts"` додати рядок:

```json
"test": "vitest run"
```

Створити `vitest.config.ts`:

```ts
import path from "node:path";
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: { environment: "node" },
  resolve: { alias: { "@": path.resolve(__dirname, "src") } },
});
```

- [ ] **Step 2: Написати тест, що падає**

Створити `src/lib/routes.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { AUTH_PATHS, isAuthPath, isProtectedPath, NAV_ITEMS } from "@/lib/routes";

describe("NAV_ITEMS", () => {
  it("містить чотири розділи платформи", () => {
    expect(NAV_ITEMS.map((i) => i.href)).toEqual([
      "/dashboard",
      "/transactions",
      "/assistant",
      "/settings",
    ]);
  });
});

describe("isProtectedPath", () => {
  it("захищає внутрішні сторінки", () => {
    expect(isProtectedPath("/dashboard")).toBe(true);
    expect(isProtectedPath("/transactions")).toBe(true);
    expect(isProtectedPath("/assistant")).toBe(true);
    expect(isProtectedPath("/settings")).toBe(true);
  });

  it("захищає вкладені шляхи", () => {
    expect(isProtectedPath("/settings/monobank")).toBe(true);
  });

  it("не захищає публічні сторінки", () => {
    expect(isProtectedPath("/")).toBe(false);
    expect(isProtectedPath("/login")).toBe(false);
    expect(isProtectedPath("/register")).toBe(false);
  });

  it("не плутає префікси з підрядками", () => {
    expect(isProtectedPath("/settingsfake")).toBe(false);
  });
});

describe("isAuthPath", () => {
  it("розпізнає сторінки входу", () => {
    expect(AUTH_PATHS).toEqual(["/login", "/register"]);
    expect(isAuthPath("/login")).toBe(true);
    expect(isAuthPath("/register")).toBe(true);
    expect(isAuthPath("/dashboard")).toBe(false);
  });
});
```

- [ ] **Step 3: Переконатися, що тест падає**

Run: `npm test`
Expected: FAIL — `Cannot find module '@/lib/routes'` (або еквівалент).

- [ ] **Step 4: Мінімальна реалізація**

Створити `src/lib/routes.ts`:

```ts
export type NavItem = { href: string; label: string };

export const NAV_ITEMS: NavItem[] = [
  { href: "/dashboard", label: "Дашборд" },
  { href: "/transactions", label: "Транзакції" },
  { href: "/assistant", label: "AI-помічник" },
  { href: "/settings", label: "Налаштування" },
];

export const AUTH_PATHS = ["/login", "/register"];

export function isProtectedPath(pathname: string): boolean {
  return NAV_ITEMS.some(
    (item) => pathname === item.href || pathname.startsWith(item.href + "/"),
  );
}

export function isAuthPath(pathname: string): boolean {
  return AUTH_PATHS.includes(pathname);
}
```

- [ ] **Step 5: Переконатися, що тести проходять**

Run: `npm test`
Expected: PASS — 6 tests passed.

- [ ] **Step 6: Commit**

```bash
git add vitest.config.ts package.json package-lock.json src/lib/routes.ts src/lib/routes.test.ts
git commit -m "feat: модуль маршрутів з тестами (Vitest)"
```

---

### Task 3: Проєкт Supabase, ключі та клієнти

**Files:**
- Create: `.env.local` (НЕ комітиться), `scripts/check-supabase.mjs`, `src/lib/supabase/client.ts`, `src/lib/supabase/server.ts`

**Interfaces:**
- Consumes: —
- Produces:
  - `createClient(): SupabaseClient` з `@/lib/supabase/client` — для клієнтських компонентів (використовують Tasks 6, 7)
  - `createClient(): Promise<SupabaseClient>` з `@/lib/supabase/server` — для серверних компонентів (використовує Task 7)
  - Змінні `.env.local`: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`

- [ ] **Step 1: 🧑 ДІЯ КОРИСТУВАЧА (Yana) — створити проєкт Supabase**

Передати Yana в чат дослівно:

> 1. Відкрий у браузері **supabase.com** і натисни зелену кнопку **Start your project**.
> 2. Зареєструйся (найпростіше — кнопкою **Continue with GitHub**, якщо є GitHub; або email + пароль).
> 3. Після входу натисни **New project**.
> 4. Заповни форму:
>    - **Name:** `webfin`
>    - **Database Password:** натисни **Generate a password** і **обовʼязково скопіюй його в нотатки** (він ще може знадобитись)
>    - **Region:** обери **Central EU (Frankfurt)** — найближче до України
> 5. Натисни **Create new project** і зачекай 1–2 хвилини, поки зникне напис про налаштування.
> 6. Напиши мені «готово», коли проєкт створиться.

Чекати на підтвердження.

- [ ] **Step 2: Створити шаблон .env.local і перевірити .gitignore**

Створити `.env.local`:

```bash
# Секрети WebFin — цей файл НЕ потрапляє в git
NEXT_PUBLIC_SUPABASE_URL=встав_сюди_Project_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=встав_сюди_anon_public_ключ
SUPABASE_SERVICE_ROLE_KEY=встав_сюди_service_role_ключ
```

Перевірити, що файл ігнорується git:

```bash
grep -n "env" .gitignore && git check-ignore .env.local
```

Expected: `.gitignore` містить рядок `.env*`, а `git check-ignore` виводить `.env.local`. Якщо ні — додати рядок `.env.local` у `.gitignore`.

- [ ] **Step 3: 🧑 ДІЯ КОРИСТУВАЧА (Yana) — вставити ключі у файл**

Передати Yana в чат дослівно:

> 1. У Supabase зліва внизу натисни іконку шестерні **Project Settings**, далі розділ **API** (може називатися **API Keys**).
> 2. Там є три значення, які треба скопіювати (кнопка копіювання праворуч від кожного):
>    - **Project URL** (починається з `https://`)
>    - ключ **anon public** (або **Publishable key** — довгий рядок)
>    - ключ **service_role** (або **Secret key** — натисни **Reveal**, щоб побачити). ⚠️ Це секретний ключ — нікому його не надсилай і не вставляй у чат.
> 3. Я вже створив файл для секретів. Виконай у терміналі (скопіюй рядок цілком):
>    `open -a TextEdit /Users/macbook/WEB/.env.local`
> 4. У файлі заміни три тексти `встав_сюди_...` на скопійовані значення (URL — у перший рядок, anon — у другий, service_role — у третій). Лапки не потрібні, пробілів навколо `=` не додавай.
> 5. Збережи файл (Cmd+S), закрий TextEdit і напиши мені «готово».

Чекати на підтвердження.

- [ ] **Step 4: Перевірити доступ до Supabase (без виводу секретів)**

Створити `scripts/check-supabase.mjs`:

```js
import { readFileSync } from "node:fs";

const env = Object.fromEntries(
  readFileSync(".env.local", "utf8")
    .split("\n")
    .filter((line) => line.includes("=") && !line.startsWith("#"))
    .map((line) => {
      const i = line.indexOf("=");
      return [line.slice(0, i).trim(), line.slice(i + 1).trim()];
    }),
);

const url = env.NEXT_PUBLIC_SUPABASE_URL;
const key = env.NEXT_PUBLIC_SUPABASE_ANON_KEY;

if (!url?.startsWith("https://") || !key || key.startsWith("встав_сюди")) {
  console.log("❌ У .env.local ще не вставлені справжні значення");
  process.exit(1);
}

const res = await fetch(`${url}/auth/v1/health`, { headers: { apikey: key } });
console.log(res.ok ? "✅ Supabase доступний, ключі працюють" : `❌ Помилка: HTTP ${res.status}`);
process.exit(res.ok ? 0 : 1);
```

Run: `node scripts/check-supabase.mjs`
Expected: `✅ Supabase доступний, ключі працюють`

- [ ] **Step 5: Встановити пакети Supabase**

```bash
npm i @supabase/supabase-js @supabase/ssr
```

- [ ] **Step 6: Клієнт для браузера**

Створити `src/lib/supabase/client.ts`:

```ts
import { createBrowserClient } from "@supabase/ssr";

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
  );
}
```

- [ ] **Step 7: Клієнт для сервера**

Створити `src/lib/supabase/server.ts`:

```ts
import { createServerClient } from "@supabase/ssr";
import { cookies } from "next/headers";

export async function createClient() {
  const cookieStore = await cookies();

  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll();
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options),
            );
          } catch {
            // виклик із серверного компонента — cookies оновить middleware
          }
        },
      },
    },
  );
}
```

- [ ] **Step 8: Перевірити збірку і закомітити**

```bash
npm run build
```

Expected: `Compiled successfully`.

```bash
git status --short
```

Expected: `.env.local` у списку ВІДСУТНІЙ (ігнорується).

```bash
git add scripts/check-supabase.mjs src/lib/supabase/client.ts src/lib/supabase/server.ts package.json package-lock.json
git commit -m "feat: підключення Supabase (клієнти для браузера і сервера)"
```

---

### Task 4: Схема бази даних і RLS

**Files:**
- Create: `supabase/schema.sql`

**Interfaces:**
- Consumes: проєкт Supabase з Task 3
- Produces: таблиці `profiles`, `monobank_tokens`, `accounts`, `transactions`, `payments` з RLS; тригер авто-створення профілю при реєстрації (на нього спирається Task 6)

- [ ] **Step 1: Створити файл схеми**

Створити `supabase/schema.sql`:

```sql
-- WebFin: схема бази даних (Етап 1)
-- Суми зберігаються в копійках (bigint); відʼємне значення = витрата.

create table public.profiles (
  id uuid primary key references auth.users (id) on delete cascade,
  email text not null,
  is_premium boolean not null default false,
  premium_until timestamptz,
  data_source text not null default 'demo' check (data_source in ('demo', 'monobank')),
  created_at timestamptz not null default now()
);

create table public.monobank_tokens (
  user_id uuid primary key references public.profiles (id) on delete cascade,
  encrypted_token text not null,
  created_at timestamptz not null default now()
);

create table public.accounts (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.profiles (id) on delete cascade,
  mono_account_id text,
  name text not null,
  currency text not null default 'UAH',
  balance bigint not null default 0,
  is_demo boolean not null default false,
  created_at timestamptz not null default now(),
  unique (user_id, mono_account_id)
);

create table public.transactions (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.profiles (id) on delete cascade,
  account_id uuid not null references public.accounts (id) on delete cascade,
  mono_id text,
  occurred_at timestamptz not null,
  description text not null,
  amount bigint not null,
  mcc integer,
  category text not null default 'Інше',
  balance_after bigint,
  is_demo boolean not null default false,
  created_at timestamptz not null default now(),
  unique (user_id, mono_id)
);

create index transactions_user_time_idx
  on public.transactions (user_id, occurred_at desc);

create table public.payments (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.profiles (id) on delete cascade,
  order_reference text not null unique,
  amount bigint not null,
  status text not null,
  created_at timestamptz not null default now()
);

-- Автоматичне створення профілю при реєстрації
create or replace function public.handle_new_user()
returns trigger
language plpgsql
security definer
set search_path = public
as $$
begin
  insert into public.profiles (id, email)
  values (new.id, coalesce(new.email, ''));
  return new;
end;
$$;

create trigger on_auth_user_created
  after insert on auth.users
  for each row execute function public.handle_new_user();

-- Row Level Security: кожен бачить лише своє
alter table public.profiles enable row level security;
alter table public.monobank_tokens enable row level security;
alter table public.accounts enable row level security;
alter table public.transactions enable row level security;
alter table public.payments enable row level security;

create policy "profiles: читати своє" on public.profiles
  for select using (auth.uid() = id);
create policy "profiles: оновлювати своє" on public.profiles
  for update using (auth.uid() = id);

-- monobank_tokens: жодних політик — доступ лише серверу через service-ключ

create policy "accounts: читати своє" on public.accounts
  for select using (auth.uid() = user_id);

create policy "transactions: читати своє" on public.transactions
  for select using (auth.uid() = user_id);

create policy "payments: читати своє" on public.payments
  for select using (auth.uid() = user_id);
```

Пояснення для виконавця: записи в `accounts`/`transactions`/`payments` робитиме лише сервер service-ключем (він обходить RLS), тому політик на insert/update для користувачів немає навмисно. На `monobank_tokens` політик немає взагалі — браузер не може прочитати токен у принципі.

- [ ] **Step 2: 🧑 ДІЯ КОРИСТУВАЧА (Yana) — виконати SQL у Supabase**

Передати Yana в чат дослівно (і вставити в чат ПОВНИЙ вміст `supabase/schema.sql` у блоці коду, щоб було що копіювати):

> 1. Відкрий свій проєкт на supabase.com.
> 2. У лівому меню натисни **SQL Editor** (іконка з символами `>_`).
> 3. Натисни **New query** (новий запит).
> 4. Скопіюй увесь код нижче і встав у велике поле.
> 5. Натисни зелену кнопку **Run** (або Cmd+Enter).
> 6. Внизу має зʼявитися **Success. No rows returned** — це означає «все створено». Напиши мені, що ти побачила.

Чекати на підтвердження. Якщо помилка — Yana копіює текст помилки в чат, виконавець виправляє SQL і повторює крок.

- [ ] **Step 3: 🧑 ДІЯ КОРИСТУВАЧА (Yana) — переконатися, що таблиці існують**

Передати Yana в чат дослівно:

> 1. У лівому меню Supabase натисни **Table Editor** (іконка таблиці).
> 2. Ти маєш побачити 5 таблиць: `accounts`, `monobank_tokens`, `payments`, `profiles`, `transactions`.
> 3. Напиши мені, чи всі пʼять на місці.

Чекати на підтвердження.

- [ ] **Step 4: Commit**

```bash
git add supabase/schema.sql
git commit -m "feat: схема бази даних з RLS і тригером створення профілю"
```

---

### Task 5: Middleware — захист роутів

**Files:**
- Create: `src/lib/supabase/middleware.ts`, `src/middleware.ts`

**Interfaces:**
- Consumes: `isProtectedPath`, `isAuthPath` з `@/lib/routes` (Task 2)
- Produces: усі запити проходять перевірку сесії; неавторизованих з захищених сторінок кидає на `/login`, авторизованих з `/login`, `/register`, `/` — на `/dashboard`

- [ ] **Step 1: Логіка оновлення сесії і редіректів**

Створити `src/lib/supabase/middleware.ts`:

```ts
import { createServerClient } from "@supabase/ssr";
import { NextResponse, type NextRequest } from "next/server";
import { isAuthPath, isProtectedPath } from "@/lib/routes";

export async function updateSession(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request });

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll();
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) =>
            request.cookies.set(name, value),
          );
          supabaseResponse = NextResponse.next({ request });
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options),
          );
        },
      },
    },
  );

  // ВАЖЛИВО: між createServerClient і getUser не має бути іншого коду —
  // інакше сесія може випадково "розлогінюватися".
  const {
    data: { user },
  } = await supabase.auth.getUser();

  const { pathname } = request.nextUrl;

  if (!user && (isProtectedPath(pathname) || pathname === "/")) {
    const url = request.nextUrl.clone();
    url.pathname = "/login";
    return NextResponse.redirect(url);
  }

  if (user && (isAuthPath(pathname) || pathname === "/")) {
    const url = request.nextUrl.clone();
    url.pathname = "/dashboard";
    return NextResponse.redirect(url);
  }

  return supabaseResponse;
}
```

- [ ] **Step 2: Підключити middleware**

Створити `src/middleware.ts`:

```ts
import { type NextRequest } from "next/server";
import { updateSession } from "@/lib/supabase/middleware";

export async function middleware(request: NextRequest) {
  return await updateSession(request);
}

export const config = {
  matcher: [
    "/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp|ico)$).*)",
  ],
};
```

- [ ] **Step 3: Перевірити редіректи вручну**

Запустити дев-сервер у фоні, потім:

```bash
curl -s -o /dev/null -w "%{http_code} -> %{redirect_url}\n" http://localhost:3000/dashboard
curl -s -o /dev/null -w "%{http_code} -> %{redirect_url}\n" http://localhost:3000/
```

Expected: обидва рядки `307 -> http://localhost:3000/login` (сесії немає, кидає на вхід). Сторінки /login ще не існує — 404 на ній зараз очікуваний, її створить Task 6.

- [ ] **Step 4: Тести не зламані + commit**

Run: `npm test`
Expected: PASS (6 tests).

```bash
git add src/middleware.ts src/lib/supabase/middleware.ts
git commit -m "feat: middleware захисту роутів через сесію Supabase"
```

---

### Task 6: Сторінки входу і реєстрації

**Files:**
- Create: `src/components/auth-form.tsx`, `src/app/(auth)/login/page.tsx`, `src/app/(auth)/register/page.tsx`
- Modify: `src/app/page.tsx`

**Interfaces:**
- Consumes: `createClient` з `@/lib/supabase/client` (Task 3); тригер профілю (Task 4); редіректи middleware (Task 5)
- Produces: маршрути `/login` і `/register`; компонент `AuthForm({ mode: "login" | "register" })`; вихід реалізує Task 7 у сайдбарі

- [ ] **Step 1: 🧑 ДІЯ КОРИСТУВАЧА (Yana) — вимкнути підтвердження email**

Щоб при реєстрації не треба було переходити за листом (для пет-проєкту зайве). Передати Yana в чат дослівно:

> 1. У Supabase у лівому меню натисни **Authentication** (іконка людини).
> 2. Далі вгорі або в підменю знайди **Sign In / Providers** (може називатися просто **Providers**).
> 3. Натисни на рядок **Email**.
> 4. Знайди перемикач **Confirm email** і ВИМКНИ його (має стати сірим).
> 5. Натисни **Save** і напиши мені «готово».

Чекати на підтвердження.

- [ ] **Step 2: Встановити іконки**

```bash
npm i lucide-react
```

- [ ] **Step 3: Спільна форма входу/реєстрації**

Створити `src/components/auth-form.tsx`:

```tsx
"use client";

import { useState } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { Wallet } from "lucide-react";
import { createClient } from "@/lib/supabase/client";

type Props = { mode: "login" | "register" };

const TEXTS = {
  login: {
    title: "Вхід у WebFin",
    button: "Увійти",
    switchText: "Ще немає акаунта?",
    switchHref: "/register",
    switchLabel: "Зареєструватися",
  },
  register: {
    title: "Реєстрація у WebFin",
    button: "Створити акаунт",
    switchText: "Вже є акаунт?",
    switchHref: "/login",
    switchLabel: "Увійти",
  },
} as const;

export function AuthForm({ mode }: Props) {
  const router = useRouter();
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [error, setError] = useState<string | null>(null);
  const [loading, setLoading] = useState(false);
  const t = TEXTS[mode];

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setLoading(true);
    setError(null);
    const supabase = createClient();

    const { error } =
      mode === "login"
        ? await supabase.auth.signInWithPassword({ email, password })
        : await supabase.auth.signUp({ email, password });

    if (error) {
      setError(
        mode === "login"
          ? "Невірний email або пароль"
          : `Не вдалося зареєструватися: ${error.message}`,
      );
      setLoading(false);
      return;
    }

    router.replace("/dashboard");
    router.refresh();
  }

  return (
    <div className="flex min-h-screen items-center justify-center bg-slate-50 px-4">
      <div className="w-full max-w-sm rounded-2xl border border-slate-200 bg-white p-8 shadow-sm">
        <div className="mb-6 flex items-center gap-2">
          <Wallet className="h-7 w-7 text-emerald-600" />
          <h1 className="text-xl font-semibold text-slate-900">{t.title}</h1>
        </div>

        <form onSubmit={handleSubmit} className="space-y-4">
          <div>
            <label
              htmlFor="email"
              className="mb-1 block text-sm font-medium text-slate-700"
            >
              Email
            </label>
            <input
              id="email"
              type="email"
              required
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              placeholder="you@example.com"
              className="w-full rounded-lg border border-slate-300 px-3 py-2 text-sm outline-none focus:border-emerald-500 focus:ring-2 focus:ring-emerald-100"
            />
          </div>

          <div>
            <label
              htmlFor="password"
              className="mb-1 block text-sm font-medium text-slate-700"
            >
              Пароль
            </label>
            <input
              id="password"
              type="password"
              required
              minLength={6}
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              placeholder="Щонайменше 6 символів"
              className="w-full rounded-lg border border-slate-300 px-3 py-2 text-sm outline-none focus:border-emerald-500 focus:ring-2 focus:ring-emerald-100"
            />
          </div>

          {error && <p className="text-sm text-red-600">{error}</p>}

          <button
            type="submit"
            disabled={loading}
            className="w-full rounded-lg bg-emerald-600 py-2 text-sm font-medium text-white transition-colors hover:bg-emerald-700 disabled:opacity-50"
          >
            {loading ? "Зачекайте…" : t.button}
          </button>
        </form>

        <p className="mt-4 text-center text-sm text-slate-500">
          {t.switchText}{" "}
          <Link
            href={t.switchHref}
            className="font-medium text-emerald-600 hover:underline"
          >
            {t.switchLabel}
          </Link>
        </p>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Сторінки /login і /register**

Створити `src/app/(auth)/login/page.tsx`:

```tsx
import { AuthForm } from "@/components/auth-form";

export default function LoginPage() {
  return <AuthForm mode="login" />;
}
```

Створити `src/app/(auth)/register/page.tsx`:

```tsx
import { AuthForm } from "@/components/auth-form";

export default function RegisterPage() {
  return <AuthForm mode="register" />;
}
```

- [ ] **Step 5: Головна сторінка — лише редірект**

Замінити ПОВНІСТЮ вміст `src/app/page.tsx` на:

```tsx
import { redirect } from "next/navigation";

export default function Home() {
  // Фактично сюди не потрапляють: middleware перенаправляє раніше.
  redirect("/dashboard");
}
```

- [ ] **Step 6: Перевірити реєстрацію вручну**

Запустити `npm run dev`, відкрити http://localhost:3000/register у браузері й зареєструвати тестового користувача `test1@webfin.local` з паролем `test123456`.

Expected: після натискання кнопки адреса змінюється на `/dashboard` і показується **404 — це очікувано** (сторінку створить Task 7; важливо, що вхід відбувся і редірект спрацював). У Supabase → Authentication → Users зʼявився користувач, у Table Editor → profiles — рядок з його email (тригер спрацював).

- [ ] **Step 7: Commit**

```bash
npm test
```

Expected: PASS.

```bash
git add src/components/auth-form.tsx "src/app/(auth)" src/app/page.tsx package.json package-lock.json
git commit -m "feat: сторінки входу і реєстрації через Supabase Auth"
```

---

### Task 7: Сайдбар і внутрішні сторінки

**Files:**
- Create: `src/components/sidebar.tsx`, `src/app/(app)/layout.tsx`, `src/app/(app)/dashboard/page.tsx`, `src/app/(app)/transactions/page.tsx`, `src/app/(app)/assistant/page.tsx`, `src/app/(app)/settings/page.tsx`

**Interfaces:**
- Consumes: `NAV_ITEMS` (Task 2), `createClient` браузерний і серверний (Task 3)
- Produces: захищена зона `(app)` з сайдбаром; чотири сторінки-заглушки, які наповнять Етапи 2–4

Примітка: якщо на момент виконання доступний Magic MCP (`mcp__magic__21st_magic_component_builder`), можна згенерувати красивіший анімований сайдбар з 21st.dev замість наведеного нижче — за умови, що він так само читає `NAV_ITEMS` з `@/lib/routes`, має кнопку «Вийти» через `supabase.auth.signOut()` і світлу тему. Наведений код — повноцінний робочий варіант за замовчуванням.

- [ ] **Step 1: Компонент сайдбара (згортається, з анімацією)**

Створити `src/components/sidebar.tsx`:

```tsx
"use client";

import { useState } from "react";
import Link from "next/link";
import { usePathname, useRouter } from "next/navigation";
import {
  Bot,
  ChevronsLeft,
  ChevronsRight,
  LayoutDashboard,
  LogOut,
  ReceiptText,
  Settings,
  Wallet,
} from "lucide-react";
import { createClient } from "@/lib/supabase/client";
import { NAV_ITEMS } from "@/lib/routes";

const ICONS: Record<string, React.ElementType> = {
  "/dashboard": LayoutDashboard,
  "/transactions": ReceiptText,
  "/assistant": Bot,
  "/settings": Settings,
};

export function Sidebar() {
  const [collapsed, setCollapsed] = useState(false);
  const pathname = usePathname();
  const router = useRouter();

  async function handleLogout() {
    const supabase = createClient();
    await supabase.auth.signOut();
    router.replace("/login");
    router.refresh();
  }

  return (
    <aside
      className={`sticky top-0 flex h-screen flex-col border-r border-slate-200 bg-white transition-all duration-300 ${
        collapsed ? "w-16" : "w-64"
      }`}
    >
      <div className="flex items-center gap-2 overflow-hidden px-4 py-5">
        <Wallet className="h-7 w-7 shrink-0 text-emerald-600" />
        {!collapsed && (
          <span className="whitespace-nowrap text-lg font-semibold text-slate-900">
            WebFin
          </span>
        )}
      </div>

      <nav className="flex-1 space-y-1 px-2">
        {NAV_ITEMS.map((item) => {
          const Icon = ICONS[item.href] ?? LayoutDashboard;
          const active =
            pathname === item.href || pathname.startsWith(item.href + "/");
          return (
            <Link
              key={item.href}
              href={item.href}
              title={item.label}
              className={`flex items-center gap-3 overflow-hidden rounded-lg px-3 py-2 text-sm font-medium transition-colors ${
                active
                  ? "bg-emerald-50 text-emerald-700"
                  : "text-slate-600 hover:bg-slate-100"
              }`}
            >
              <Icon className="h-5 w-5 shrink-0" />
              {!collapsed && (
                <span className="whitespace-nowrap">{item.label}</span>
              )}
            </Link>
          );
        })}
      </nav>

      <div className="space-y-1 border-t border-slate-200 p-2">
        <button
          onClick={handleLogout}
          title="Вийти"
          className="flex w-full items-center gap-3 overflow-hidden rounded-lg px-3 py-2 text-sm font-medium text-slate-600 transition-colors hover:bg-slate-100"
        >
          <LogOut className="h-5 w-5 shrink-0" />
          {!collapsed && <span className="whitespace-nowrap">Вийти</span>}
        </button>
        <button
          onClick={() => setCollapsed(!collapsed)}
          title={collapsed ? "Розгорнути меню" : "Згорнути меню"}
          className="flex w-full items-center gap-3 overflow-hidden rounded-lg px-3 py-2 text-sm font-medium text-slate-600 transition-colors hover:bg-slate-100"
        >
          {collapsed ? (
            <ChevronsRight className="h-5 w-5 shrink-0" />
          ) : (
            <ChevronsLeft className="h-5 w-5 shrink-0" />
          )}
          {!collapsed && <span className="whitespace-nowrap">Згорнути</span>}
        </button>
      </div>
    </aside>
  );
}
```

- [ ] **Step 2: Захищений layout із сайдбаром**

Створити `src/app/(app)/layout.tsx`:

```tsx
import { redirect } from "next/navigation";
import { Sidebar } from "@/components/sidebar";
import { createClient } from "@/lib/supabase/server";

export default async function AppLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // Другий рубіж захисту на додачу до middleware
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) redirect("/login");

  return (
    <div className="flex min-h-screen bg-slate-50">
      <Sidebar />
      <main className="flex-1 p-8">{children}</main>
    </div>
  );
}
```

- [ ] **Step 3: Чотири сторінки-заглушки**

Створити `src/app/(app)/dashboard/page.tsx`:

```tsx
export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-slate-900">Дашборд</h1>
      <div className="mt-6 rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center text-slate-500">
        Тут зʼявляться картки-підсумки та графіки витрат (Етап 2)
      </div>
    </div>
  );
}
```

Створити `src/app/(app)/transactions/page.tsx`:

```tsx
export default function TransactionsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-slate-900">Транзакції</h1>
      <div className="mt-6 rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center text-slate-500">
        Тут буде список транзакцій і кнопка «Оновити з Monobank» (Етап 2)
      </div>
    </div>
  );
}
```

Створити `src/app/(app)/assistant/page.tsx`:

```tsx
export default function AssistantPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-slate-900">AI-помічник</h1>
      <div className="mt-6 rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center text-slate-500">
        Тут буде чат про ваші витрати (Етап 4)
      </div>
    </div>
  );
}
```

Створити `src/app/(app)/settings/page.tsx`:

```tsx
export default function SettingsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-slate-900">Налаштування</h1>
      <div className="mt-6 rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center text-slate-500">
        Тут буде підключення Monobank, демо-дані та Преміум (Етапи 2–3)
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Перевірити в браузері**

Запустити `npm run dev`, відкрити http://localhost:3000/login, увійти як `test1@webfin.local` / `test123456`.

Expected:
- відкривається /dashboard із сайдбаром зліва (логотип WebFin, 4 пункти меню)
- кліки по пунктах меню перемикають сторінки, активний пункт підсвічений зеленим
- кнопка «Згорнути» плавно звужує сайдбар до іконок і назад
- кнопка «Вийти» повертає на /login, після чого /dashboard знову недоступний (редірект на /login)

- [ ] **Step 5: Збірка, тести, commit**

```bash
npm run build && npm test
```

Expected: збірка успішна, 6 тестів PASS.

```bash
git add src/components/sidebar.tsx "src/app/(app)"
git commit -m "feat: сайдбар і захищені сторінки-заглушки"
```

---

### Task 8: Фінальна перевірка і README

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: усе з Tasks 1–7
- Produces: перевірений каркас Етапу 1; README з інструкцією запуску

- [ ] **Step 1: Переписати README**

Замінити ПОВНІСТЮ вміст `README.md` на:

```markdown
# WebFin — аналітика витрат

Веб-платформа фінансової аналітики: транзакції з Monobank, дашборд із
графіками, преміум через WayForPay і AI-помічник.

- Специфікація: `docs/superpowers/specs/2026-07-15-webfin-design.md`
- Стек: Next.js + TypeScript + Tailwind, Supabase (Postgres + Auth)

## Запуск

1. Потрібен файл `.env.local` з ключами Supabase (див. специфікацію).
2. `npm install`
3. `npm run dev` → http://localhost:3000

## Команди

- `npm run dev` — дев-сервер
- `npm run build` — продакшн-збірка
- `npm test` — юніт-тести
- `node scripts/check-supabase.mjs` — перевірка доступу до Supabase

## Статус

Етап 1 «Каркас» готовий: реєстрація/вхід, сайдбар, захищені сторінки.
Далі — Етап 2 «Дані й дашборд».
```

- [ ] **Step 2: Повна перевірка**

```bash
npm test && npm run build && node scripts/check-supabase.mjs
```

Expected: тести PASS, збірка успішна, `✅ Supabase доступний`.

- [ ] **Step 3: 🧑 ДІЯ КОРИСТУВАЧА (Yana) — приймальна перевірка**

Запустити `npm run dev` і передати Yana в чат дослівно:

> Етап 1 готовий — перевір, будь ласка, сама 🙂
> 1. Відкрий у браузері http://localhost:3000 — тебе має перекинути на сторінку входу.
> 2. Натисни «Зареєструватися» і створи акаунт зі СВОЇМ email і паролем.
> 3. Після реєстрації ти маєш опинитися на Дашборді з меню зліва.
> 4. Потицяй меню: Дашборд, Транзакції, AI-помічник, Налаштування — усі сторінки мають відкриватися.
> 5. Натисни «Згорнути» внизу меню — воно має плавно зʼїхатися до іконок.
> 6. Натисни «Вийти» — маєш повернутися на сторінку входу.
> Напиши, що спрацювало, а що ні.

Чекати на підтвердження.

- [ ] **Step 4: Фінальний commit**

```bash
git add README.md
git commit -m "docs: README з інструкцією запуску (Етап 1 завершено)"
```
